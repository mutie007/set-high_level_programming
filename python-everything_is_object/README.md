# Python - Everything is Object

This project explores Python's object model: how variables are references to objects, mutability vs immutability, identity vs equality, and CPython-specific behaviors like string interning.
